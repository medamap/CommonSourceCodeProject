# Phase 5 実環境実装エージェント指示書

## エージェント情報
- **エージェント名**: RealEnvAgent-Phase5-Production
- **役割**: 実エミュレータ環境でのMB8877互換レイヤー実用化
- **作成日時**: 2025/06/12
- **作成者**: PMエージェント
- **優先度**: 最高
- **推定作業時間**: 7-10日

## 背景・現状分析

### 📊 Phase 4での判明事項
- **Type III Commands**: 重複定義により実行不可（実環境での解決必要）
- **モック環境の限界**: 複雑なイベント処理の実装困難
- **現在の達成度**: 基本機能50%レベル（実用化には不十分）

### 🎯 Phase 5の使命
**実エミュレータ環境でMB8877互換レイヤーを実用レベルまで完成**
- 実際のFM7/X1エミュレータでの動作確認
- 実ディスクイメージでの互換性達成
- プロダクションレディな品質確保

## 実行する作業

### 🔧 Phase 5-A: 実環境構築（Day 1-2）

#### 1. エミュレータビルド環境準備
**FM7エミュレータ環境**:
```bash
# FM7エミュレータのビルド準備
cd /path/to/fm7-emulator
cp /Volumes/PoppoSSD2T/Projects/ClaudeCodeProjects/Fdc-Porting/CommonSourceCodeProject/src/vm/mb8877_compat.* src/vm/
# 既存のmb8877.cpp/hをバックアップ後に置き換え
```

**X1エミュレータ環境**:
```bash
# X1エミュレータのビルド準備
cd /path/to/x1-emulator
# 同様の手順で置き換え
```

#### 2. ビルドシステム統合
- Visual Studio プロジェクト（.vcxproj）への統合
- Makefile/CMakeへの統合
- 依存関係の解決

#### 3. 初期ビルドとエラー解決
- コンパイルエラーの修正
- リンクエラーの解決
- 実環境特有の問題対応

### 🛠️ Phase 5-B: Type III Commands修正（Day 3-4）

#### 1. 重複定義の解消
**問題の修正**:
```cpp
// mb8877_compat.cpp内の重複定義を特定・削除
// 以下のような重複を解消：
// - cmd_readaddr() の重複
// - cmd_readtrack() の重複
// - cmd_writetrack() の重複
```

#### 2. 実環境でのイベント処理実装
- 実際のイベントマネージャーとの統合
- タイミング制御の精密化
- ディスクI/O処理の実装

#### 3. Type III Commands完全実装
- **Read Address**: ID フィールド読み取り機能
- **Read Track**: トラック全体の読み取り
- **Write Track**: フォーマット機能

### 📈 Phase 5-C: 段階的品質向上（Day 5-8）

#### Stage 1: 基本動作確認（現在50% → 70%目標）
**重点項目**:
1. **基本的なディスク読み込み**
   - セクタ読み取りの安定動作
   - エラーなしでのブート確認
   - 基本的なファイルアクセス

2. **主要コマンドの動作確認**
   - Type I: シーク動作の確認
   - Type II: 読み書きの基本動作
   - Type IV: 割り込み処理

**確認方法**:
```bash
# 実際のディスクイメージでテスト
./fm7emu -disk basic.d88
# BASICが正常起動することを確認
```

#### Stage 2: 実用レベル達成（70% → 85%目標）
**重点項目**:
1. **エラーハンドリング完成**
   - CRCエラー処理
   - Record Not Found処理
   - タイムアウト処理

2. **互換性向上**
   - 各種ディスクフォーマット対応
   - プロテクトチェック
   - 特殊フォーマット基本対応

**確認方法**:
```bash
# 複数のディスクイメージでテスト
./fm7emu -disk game1.d88  # ゲームディスク
./fm7emu -disk app1.d88   # アプリケーション
./fm7emu -disk data1.d88  # データディスク
```

#### Stage 3: 完全互換性（85% → 95%目標）
**重点項目**:
1. **特殊ディスク対応**
   - FM7 RIGLAS形式
   - X1 Batten Tanuki形式
   - コピープロテクト対応

2. **タイミング完全互換**
   - 実機同等のタイミング
   - DRQ/IRQの精密制御
   - シーク音再生

### 🧪 Phase 5-D: 実環境テスト（Day 9-10）

#### 1. 互換性テストスイート
**テスト項目**:
```
□ BASIC起動テスト
□ システムディスク読み込み
□ ゲームディスク（10種類以上）
□ アプリケーション（5種類以上）
□ データディスク読み書き
□ フォーマット機能
□ コピー機能
□ 特殊ディスク（RIGLAS等）
```

#### 2. パフォーマンステスト
- ディスクアクセス速度測定
- CPU使用率確認
- メモリ使用量測定

#### 3. 長時間動作テスト
- 24時間連続動作確認
- メモリリーク検査
- 安定性確認

## 期待される成果物

### 1. **実用版実装ファイル**
- `src/vm/mb8877.cpp` (実環境対応版)
- `src/vm/mb8877.h` (必要な調整済み)
- 重複定義解消、実環境統合完了

### 2. **ビルド成果物**
- FM7エミュレータ実行ファイル（MB8877互換）
- X1エミュレータ実行ファイル（MB8877互換）
- ビルド手順書

### 3. **テスト結果証明**
- 実ディスクイメージでの動作確認結果
- 互換性テスト結果一覧
- パフォーマンス測定結果

### 4. **完成レポート**
- `docs/reports/phase5-production-report.md`
- 実環境での達成度詳細
- 既知の問題と対策
- 今後の拡張提案

## 実装ガイドライン

### 1. 実環境での開発手法
```cpp
// 実環境でのデバッグ出力
#ifdef _DEBUG
#define FDC_DEBUG_LOG(fmt, ...) \
    this->out_debug_log(_T("MB8877: ") fmt, ##__VA_ARGS__)
#else
#define FDC_DEBUG_LOG(fmt, ...)
#endif
```

### 2. 実ディスクI/O処理
```cpp
// 実際のDISKクラスとの連携
bool MB8877::read_sector_data() {
    if (!disk[drvreg]->get_sector(track, side, sector)) {
        status |= S_RNF;
        return false;
    }
    // 実データの読み取り処理
    return true;
}
```

### 3. イベント処理の実装
```cpp
// 実環境のイベントマネージャー活用
void MB8877::register_seek_event() {
    double usec = calculate_seek_time();
    register_event(this, EVENT_SEEK, usec, false, &seek_event_id);
}
```

## 作業進行管理

### 📅 Daily Milestone

#### Day 1-2: 実環境構築
- [ ] エミュレータソース取得
- [ ] ビルド環境構築
- [ ] 初期統合とビルド成功

#### Day 3-4: Type III Commands修正
- [ ] 重複定義解消
- [ ] イベント処理実装
- [ ] Type III Commands動作確認

#### Day 5-6: 基本動作達成（70%）
- [ ] BASIC起動成功
- [ ] 基本的な読み書き動作
- [ ] 主要コマンド動作確認

#### Day 7-8: 実用レベル達成（85%）
- [ ] エラーハンドリング完成
- [ ] 各種ディスク対応
- [ ] 安定動作確認

#### Day 9-10: 完全互換性とテスト
- [ ] 特殊ディスク対応
- [ ] 互換性テスト完了
- [ ] リリース準備完了

## 成功基準

### 必須達成項目
1. **実環境動作**: FM7/X1エミュレータでの正常動作
2. **基本互換性**: システムディスク起動成功
3. **実用性**: 主要ソフトウェアの動作確認

### 品質基準
1. **安定性**: 24時間連続動作
2. **互換性**: 85%以上のディスクイメージ対応
3. **性能**: 実機同等以上の速度

## Phase 6への展望

### 次期拡張項目
1. **特殊ディスク完全対応**: プロテクト含む100%互換
2. **サウンド機能**: シーク音、モーター音の再現
3. **高度な最適化**: さらなるパフォーマンス向上

この指示書に従い、実環境でMB8877互換レイヤーを実用レベルまで完成させてください。モック環境の限界を超え、実際のエミュレータでの動作を通じて真の互換性を達成します。