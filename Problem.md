# MB8877 FDC 互換実装の問題分析レポート

**更新日時**: 2025-06-16 07:58  
**現在のビルド**: mb8877_compat版（`-D_MB8877_COMPAT`）

## 🎯 **プロジェクトの目的**
GPL-licensed `mb8877.cpp` を BSD-3-Clause licensed `mb8877_compat.cpp` に置き換え、X1turboエミュレータで完全な互換動作を実現する。

## ✅ **解決済みの問題**

### 1. DRQ処理の問題（解決済み）
- **問題**: DRQが条件付きでしか設定されていなかった
- **解決**: EVENT_DRQで無条件にDRQ設定するよう修正

### 2. RESTOREコマンドの初期化問題（解決済み）  
- **問題**: `trkreg`の初期化がオリジナルと異なっていた
- **解決**: `trkreg = 0xff`に修正してオリジナルと同じ動作に

### 3. X1仕様準拠の問題（解決済み）
- **問題**: S_HLDビット（ヘッドロード）が設定されていた
- **解決**: X1ではbit 5は無視される仕様のため、S_HLDビット設定を削除

### 4. S_NRDYビット管理の問題（解決済み）
- **問題**: S_NRDYビットが適切にクリアされていなかった  
- **解決**: `update_ready()`と`cmd_process()`でのS_NRDY管理を修正

### 5. STEPコマンドの実装バグ（解決済み）
- **問題**: `seekvct = false`の時にヘッドが移動しなかった
- **解決**: `cmd_step()`でseekvcに基づいて正しくIN/OUT方向に移動するよう修正

## ⚠️ **現在の問題**

### **ブートプロセスの進行停止**
- **症状**: セクタ1→2の遷移は成功するが、トラック0を超えた進行ができない
- **期待動作**: オリジナル版では47セクタを複数トラック（0,1,2...）にわたって読み込み
- **実際の動作**: compat版では同じトラック0のセクタ1,2を繰り返し読み込み
- **結果**: X1 IPLが完了せず、アプリケーションが起動しない

## 🔍 **現在取り組んでいること**

### **トラック移動ロジックの調査**
以下の処理でオリジナル版とcompat版の差異を特定する必要がある：

1. **マルチセクタ読み込みの境界処理**
   - セクタ16読み込み後、次のトラックに移動するタイミング
   - EVENT_MULTI2での継続条件判定

2. **SEEK/STEPコマンドの発生条件**  
   - いつトラック移動コマンドが実行されるか
   - セクタが見つからない時の処理

3. **トラック境界での動作**
   - 現在のトラックでセクタが見つからない場合の処理
   - 次のトラックへの移行判定

## 🛠️ **次のアクションプラン**

### Phase 1: ログ比較調査
1. **オリジナル版**（`-D__MB8877_COMPAT`）でのログ採取
   - トラック移動がどのタイミングで発生するかを確認
   - 47セクタの読み込みパターンを記録

2. **compat版**（`-D_MB8877_COMPAT`）でのログ採取  
   - オリジナルとの差異を特定
   - トラック移動が発生しない理由を突き止める

### Phase 2: 差異の修正
調査結果に基づいて、トラック移動ロジックの不整合を修正

### Phase 3: 最終テスト
完全なブートプロセスの動作確認

## 📊 **技術的詳細**

### 確認済みの動作
- ✅ セクタ1のデータ読み込み（"turbo A" 正常取得）
- ✅ セクタ1→2の遷移
- ✅ DRQサイクルの完全動作（256バイト/セクタ）
- ✅ EVENT_MULTI1/MULTI2の基本機能

### 未解決の動作
- ❌ トラック0を超えた読み込み進行
- ❌ 47セクタ全体の読み込み完了  
- ❌ X1 IPLブートプロセスの完了
- ❌ アプリケーション起動

## 🎯 **成功の定義**
mb8877_compat版で以下が達成されること：
1. X1turboの完全ブート
2. ディスクからのアプリケーション起動
3. オリジナル版と同等の互換性

---

**現在の状況**: トラック移動ロジックの調査段階  
**次の作業**: オリジナル版とcompat版のログ比較分析